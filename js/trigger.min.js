var ua = navigator.userAgent;
if(ua.indexOf("iPhone")>0||ua.indexOf("iPad")>0||ua.indexOf("iPod")>0||ua.indexOf("Android")>0||ua.indexOf("Kindle")>0);
else{
  skrollr.init({forceHeight: !1,smoothScrolling: !0});
};
